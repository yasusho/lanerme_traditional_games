# タスク：相乗りルールの修正

- [x] `simulator/game.js` の `_initGameLogic` にスタック初期化処理を追加する
- [x] `simulator/game.js` に `getPassengers` と `updateNodeStacks` メソッドを実装する
- [x] `simulator/game.js` の `executeMove` 内の相乗りロジックを修正する
- [x] `simulator/game.js` の `executeAITurn` 内の相乗りロジックを修正する
- [x] `simulator/game.js` の `renderMap` をスタック順序に対応させる
- [x] ブラウザで挙動を確認し、スタックの下のプレイヤーが移動した時に上のプレイヤーだけが運ばれることを確認する
